# 复制本文件为 .env 并填入你的 DeepSeek key（.env 已 gitignore，不会入库）
# 未配置时，报告仍会输出（规则化兜底解读）；配置后「投资专家解读」由 DeepSeek 生成，更深入。
DEEPSEEK_API_KEY=sk-替换成你的key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat